#include <iostream>
#include <string>

/**
 * === is a dash
 * = is a dit
 * symbols are separated by a dot (.)
 * words are separated by .......
 * letters are separated by ...
 */

int main() {

    int test_case = 0;
    std::string encoded_text;
    
    std::cin >> n;
    
    while (test_case > 0) {
	std::cin >> encoded_text;

	
	
	test_case--;
    }
    
    return 0;
}
